// import "react-native";
// import * as React from "react";
// jest.mock("Dimensions");
// import {MainView} from "../src/view/main_view";
// import {peripherals} from "../__mocks__/mock_peripherals_declaration";
//
// // Note: test renderer must be required after react-native.
// import * as renderer from "react-test-renderer";


test("renders correctly", () => {
	return;
	// const tree = renderer.create(<MainView
	// 	peripherals={peripherals}
	// 	onPressTile={(viewPeripheral) => {console.log(viewPeripheral);}}
	// 	subscribeToLayoutChange={(_callback, id) => {console.log(id);}}
	// 	unsubscribeFromLayoutChange={(id) => {console.log(id);}}
	// />).toJSON();
	// expect(tree).toMatchSnapshot();
});