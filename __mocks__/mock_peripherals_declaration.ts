import {DefaultValues} from "../src/defaults/default_values";
import {PeripheralPartsContainer} from "../src/types";

let peripherals: Array<PeripheralPartsContainer> = [];

peripherals.push(DefaultValues.PERIPHERAL_PARTS_CONTAINER);

export {peripherals};